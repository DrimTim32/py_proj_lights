language: python
sudo: required
python:
  - 3.4
branches:
  only:
    - malin-development
install:
 - sudo apt-get install python-pygame 
# command to run tests
script:
  python Tests/all_tests.py
sudo : required